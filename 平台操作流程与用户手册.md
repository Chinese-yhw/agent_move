# AI 短剧生成平台 — 当前实现流程 & 用户操作手册

> 版本：v2.0（实现版）
> 日期：2026-09-11
> 说明：本文档描述**当前代码已实现的真实流程**，并站在**使用者角度**给出每一步的具体操作指引。与设计蓝图《短剧生成产品设计文档.md》的区别在于：那份是"打算怎么做"，这份是"现在实际怎么跑、你该怎么点"。

---

## 〇、一句话理解这个平台

把一份**剧本**自动变成一部**短剧成片**。中间经过：拆成分镜 → 提取角色/场景资产 → 锁定每个资产的"视觉锚点+标准照" → 基于锚点为每个镜头生成首帧图 → 首帧图变视频 → 配音 → FFmpeg 合成。

核心理念（工业级做法）：**人物不是一句提示词，而是下一集还能认出来的资产系统。** 所有镜头都从"不可变的视觉锚点"出发，而不是每次自由发挥。

---

## 一、技术栈现状（实际用的，不是设计稿）

| 层 | 实际选型 | 备注 |
|---|---|---|
| 后端 | FastAPI + SQLAlchemy 2.0（async） | [app/main.py](file:///f:/github/agent_move/backend/app/main.py) |
| 数据库 | **SQLite**（开发默认，零安装） | `./data/drama_gen.db`；生产可换 PostgreSQL |
| 异步任务 | **Celery + Redis**；无 Redis 时自动退化 eager 线程模式 | [celery_app.py](file:///f:/github/agent_move/backend/app/tasks/celery_app.py) |
| 文生图 | ComfyUI + Z-Image-Turbo / SDXL | AutoDL GPU 实例 |
| 角色一致性 | **视觉锚点(identity_anchor)** + IP-Adapter/FaceID + LoRA | 三层策略 |
| 图生视频 | ComfyUI + Wan2.1 I2V / T2V | |
| LLM | DeepSeek（OpenAI 兼容协议） | 需配 API Key |
| TTS | CosyVoice（未配时用 **edge-tts 免费兜底**） | |
| 合成 | FFmpeg | |
| 前端 | React 18 + Vite + TypeScript | [frontend/src/App.tsx](file:///f:/github/agent_move/frontend/src/App.tsx) |

### 启动方式（三个终端）
```powershell
# 终端1 后端
cd backend
.\.venv\Scripts\uvicorn app.main:app --port 8000

# 终端2 Celery worker（装了 Redis 才需要；没装则后端 eager 模式自动跑任务）
cd backend
.\.venv\Scripts\celery -A app.tasks.celery_app.celery_app worker --loglevel=info --pool=solo

# 终端3 前端
cd frontend
npm run dev
```
打开 http://localhost:5173/ 进入平台。

> ⚠️ 改了后端代码或 `.env` 后，必须重启 uvicorn 和 worker 才生效（它们不会热重载）。

---

## 二、整体流程图（当前实现）

```
① 新建项目（选画风）
        │
        ▼
② 📋 剧本页：建剧本 → 粘贴正文 → AI生成分镜 / 上传分镜表 → 批量提交
        │
        ▼
③ 🔍 检测页：LLM 按 7 维度审查剧本+分镜，给修改建议（可选）
        │
        ▼
④ 🧩 素材页：提取素材（角色/场景/道具）→ 每个资产生成【视觉锚点 identity_anchor】
        │       （这是全剧不可变的身份特征，是"下一集还认得出"的关键）
        ▼
⑤ 🖼 标准照页：基于锚点生成候选图 → 挑一张「设为标准照」(锁定)
        │       （可选：给角色设 LoRA，一致性更强）
        ▼
⑥ 🎬 镜头页：生成首帧图（prompt = 风格+锚点+本镜动作，强制注入参考图）
        │       → 选首帧 → 生成视频(I2V) → 审核通过
        ▼
⑦ 🔊 配音页：批量 TTS（edge-tts 兜底，按角色分配音色）
        │
        ▼
⑧ 🎞 合成页：FFmpeg 拼接已通过镜头 + 配音 + 字幕 → 导出 mp4
        │
        ▼
⑨ 💰 成本页：按任务类型汇总 GPU 费用
```

左侧导航顺序即推荐操作顺序：📋剧本 → 🔍检测 → 🧩素材 → 🖼标准照 → 🎬镜头 → 🔊配音 → 🎞合成 → 💰成本。

---

## 三、逐步操作详解（用户视角）

### 第 0 步：新建项目
- **在哪**：首页 [ProjectsPage](file:///f:/github/agent_move/frontend/src/pages/ProjectsPage.tsx)
- **怎么做**：输入「项目名称」+ 选「画风」（写实电影感 / 国漫 / 日系动画 / 赛博朋克 / 古风水墨）→ 创建
- **为什么重要**：画风会自动拼在所有生图 prompt 最前面，决定整部片子的视觉调性。
- **顶部 GPU 灯**：每 15 秒探测 ComfyUI 是否可达。**绿灯才能生图/生视频**，红灯时所有生成任务会失败。

### 第 1 步：📋 剧本与分镜
- **在哪**：[ScriptPage](file:///f:/github/agent_move/frontend/src/pages/ScriptPage.tsx)
- **操作序列**：
  1. 「＋新建剧本」→ 填集数、标题、**正文（直接粘贴文本）** → 创建
  2. 选中剧本 → 点 **「🤖 AI 生成分镜」**：DeepSeek 把正文拆成 12~20 个分镜草稿写入下方表格（覆盖旧分镜）
  3. 或用底部 **「上传分镜表 / CSV」** 导入（格式见第四节）
  4. 表格可逐行手改 → 最后点 **「批量提交分镜」** 入库
- **每个分镜字段**：镜号、场景、画面描述、正面/负面 Prompt、动作提示词、时长(3~8s)、运镜、景别、台词。

### 第 2 步：🔍 内容检测（可选但推荐）
- **在哪**：[DetectPage](file:///f:/github/agent_move/frontend/src/pages/DetectPage.tsx)
- **操作**：点「开始检测」→ 查看错误/警告清单和修改建议
- **7 个维度**：角色一致性、场景连续性、道具闭环、台词归属、时长合理性、景别运镜完整性、剧情逻辑
- **依赖**：需要 `LLM_API_KEY`（否则报 502）

### 第 3 步：🧩 素材提取 + 视觉锚点（核心！）
- **在哪**：[AssetsPage](file:///f:/github/agent_move/frontend/src/pages/AssetsPage.tsx)，右上角先**选剧本**
- **两种建素材方式**：
  - **「提取素材」**：LLM 从剧本+分镜抽取角色/场景/道具，**同时为每个角色/场景生成英文「身份锚点 identity_anchor」**
  - **「上传素材清单 txt」**：解析你写好的清单（格式见第四节），含【镜号N】块还会自动绑定到镜头；清单里写了 `锚点：`/`identity_anchor：` 行会自动存入身份锚点，无需手动补
- **关键操作·编辑锚点**：点素材卡片的「编辑」→ 在黄框里维护「🔒 身份锚点」。
  - 锚点只写**恒定不变的身份特征**：性别年龄段、脸型、眼睛、发型发色、标志物、固定服装
  - **不要**写表情/动作/背景/光线（那些属于具体镜头）
  - 例：`fictional young man, square jaw, sharp dark eyes, black hair tied in topknot, plain teal ancient robe`
- **为什么这步最重要**：锚点是后续所有镜头首帧的"身份约束源"。没有它，模型就会自由发挥导致换脸/换衣/换画风。

### 第 4 步：🖼 标准照生成与锁定
- **在哪**：[ImagesPage](file:///f:/github/agent_move/frontend/src/pages/ImagesPage.tsx)
- **操作序列**：
  1. （角色可选）「➕上传参考脸」PNG/JPG → 走 FaceID/IP-Adapter 锁长相
  2. （角色可选）「设置 LoRA」→ 若你已在 ComfyUI 训好该角色 LoRA，填文件名，一致性最强
  3. 点 **「生成候选 ×4」** → 基于"风格+锚点+描述"出 4 张
  4. 挑一张 **「设为标准照」** → 状态变**已锁定**，成为下游镜头的参考基准
- **提醒**：标准照本身也会注入锚点，所以一定要先把锚点填对。

### 第 5 步：🎬 镜头生成与审核
- **在哪**：[ShotsPage](file:///f:/github/agent_move/frontend/src/pages/ShotsPage.tsx)
- **操作序列（每个镜头）**：
  1. **「🖼 生成首帧图 ×2」**：prompt = `{风格} + {关联角色锚点} + {关联场景锚点} + {本镜描述}`，并用已锁定标准照做 IP-Adapter 参考
  2. 挑一张 **「选为首帧」**；不满意的候选可 **「🗑删除」**
  3. **「🎬 生成视频 ×2」**：Wan2.1 I2V 让首帧动起来；若无首帧可勾 **「特效模式(T2V)」** 跳过首帧（适合黑屏/空镜/特效）
  4. 视频进「待审核」→ 挑一条 **「通过」**，其余自动淘汰
- **首帧选取优先级**：镜头专属首帧图 > 手动指定素材标准照 > 关联角色标准照 > 场景标准照
- **右下角「首帧素材」下拉**：自动匹配不到时可手动指定用哪个素材的标准照当首帧

### 第 6 步：🔊 配音
- **在哪**：[TtsPage](file:///f:/github/agent_move/frontend/src/pages/TtsPage.tsx)
- **操作**：点「批量配音」→ 对全部分镜台词做 TTS；可逐条试听、删除
- **当前实现**：CosyVoice 地址未配时用 **edge-tts 免费兜底**，按角色名自动分配中文字色、按情绪微调语速音调

### 第 7 步：🎞 合成导出
- **在哪**：[ComposePage](file:///f:/github/agent_move/frontend/src/pages/ComposePage.tsx)
- **操作**：勾选是否「烧录字幕」→ 点「导出成片」→ 下载 mp4
- **规则**：FFmpeg 把所有**已通过**的镜头按镜号拼接 + 混入配音 + 烧字幕。**至少 1 个通过镜头**才能合成。

### 第 8 步：💰 成本
- **在哪**：[CostsPage](file:///f:/github/agent_move/frontend/src/pages/CostsPage.tsx)
- 按任务类型汇总 GPU 费用（AutoDL 时租折算），盯住成本目标。

---

## 四、上传文件格式速查

平台有 4 个上传入口，文本文件编码自动识别（UTF-8 BOM → GBK 回退）。

| 入口 | 位置 | 格式 |
|---|---|---|
| 上传分镜表 | 剧本页 | `.txt` / `.md` |
| 上传 CSV 提示词 | 剧本页 | `.csv` / `.txt`（内容按 CSV 解析） |
| 上传素材清单 | 素材页 | `.txt` |
| 上传参考脸 | 标准照页 | PNG / JPG |

### ① 分镜表 txt/md（自动识别 2 种）
**A. 表格格式**（Tab / Markdown / 2+空格分隔，含"镜号"表头）：
```
镜号	景别	时长	画面描述	台词/内心独白	运镜/音效提示
1	中景	5s	林夏推门进入咖啡馆	林渊：我来了。	跟拍
2	特写	3s	阿瑶抬头	阿瑶：（紧张）师父人呢？	固定
```
**B. 段落式 Markdown 分镜拆解**：
```
**镜1｜0-5s 开篇：黑屏**
**剧作目的**：交代末世背景
**情感基调**：死寂压抑
**视觉风格**：暗黑高对比
**运镜理由**：固定静置
长老：（低沉）预言成真了。
```

### ② CSV 提示词表（给镜头补正/负 Prompt）
支持 `.csv` 和 `.txt` 两种扩展名——**只要内容是 CSV 格式即可**（按内容解析，不看后缀）。
```csv
镜号,正面 Prompt,负面 Prompt
1,"写实电影感，少年林渊站在山门前，黑发长袍","低质量，模糊，变形"
2,"传承石室全景，石台凹槽发光","水印，文字"
```

### ③ 素材清单 txt（自动识别 2 种）
**A. 章节式**（可写 `锚点：`/`identity_anchor：` 字段，自动入库）：
```
角色
林渊
性别：男
主体：十六岁少年，剑眉星目，青色道袍
锚点：fictional young man, square jaw, sharp dark eyes, black hair in topknot, plain teal ancient robe
变体：受伤版：嘴角带血，道袍破损
音色描述：清亮少年音
```
> 变体会自动继承主体的锚点（同一角色的不同状态身份特征一致）。
**B. 标准照提示词式**（推荐，能自动关联镜头；块内用 `锚点：` 行写英文身份锚点）：
```
【镜号1】
林渊
守忆宗山门
【林渊·标准照】
锚点：fictional young man, square jaw, black hair in topknot, plain teal ancient robe
写实电影感，十六岁少年定妆照，黑发束冠，青色道袍……
【守忆宗山门·标准照】
锚点：ancient chinese stone sect gate, carved stone plaque, mountain backdrop
国风仙侠，宗门山门概念图，石匾高悬……
```
> 注：清单里写了 `锚点：`/`identity_anchor：` 行就会自动存入 identity_anchor，**无需再去素材页手动补**。没写的素材才需要手动补或改用「提取素材」让 LLM 生成。

### ④ 参考脸图
任意 PNG/JPG 人像，存到 `media/reference/`。

---

## 五、角色一致性：三层策略（本平台的核心机制）

这是"为什么别人能做出一致短剧、我们之前不行"的答案。当前实现采用分层：

| 层级 | 手段 | 解决什么 | 强度 |
|---|---|---|---|
| 第一层 | **视觉锚点 identity_anchor**（英文结构化身份描述，全剧不可变，强制拼进每个镜头 prompt） | 身份词根本没进 prompt 的断点 | 必做，地基 |
| 第二层 | **IP-Adapter / FaceID**（已锁定标准照作参考图注入） | 脸部/画风相似 | 自动，锁标准照后生效 |
| 第三层 | **角色 LoRA**（每角色一个 .safetensors，挂载生成） | 跨镜头焊死同一形象 | 可选，效果最强 |

**首帧 prompt 组装公式**（[generation.py](file:///f:/github/agent_move/backend/app/tasks/generation.py#L218-L223)）：
```
full_prompt = {项目风格} + {角色锚点(去重)} + {场景锚点} + {本镜 image_prompt/description}
```
所有镜头共享同一段锚点 → 这就是"下一集还认得出"的实现。

---

## 六、环境配置检查清单（跑通全流程的前置条件）

对照 [.env](file:///f:/github/agent_move/backend/.env)：

| 配置项 | 当前值 | 影响的功能 | 是否就绪 |
|---|---|---|---|
| `DATABASE_URL` | SQLite | 全部 | ✅ |
| `REDIS_URL` | `redis://:123456@localhost:6379/0` | Celery 异步 | ✅（需本地 Redis 运行） |
| `COMFYUI_BASE_URL` | AutoDL 公网地址 | 生图/生视频 | ⚠️ 需 GPU 实例开机、ComfyUI 在跑 |
| `ip-adapter-plus_sdxl_vit-h.safetensors` | 已下载 809M | 双链路锁服装画风 | ✅ |
| `LLM_API_KEY` | **`sk-REPLACE_ME` 占位符** | AI生成分镜/检测/提取素材/翻译 | ❌ **必须换成真实 DeepSeek key** |
| `TTS_BASE_URL` | 占位符 | CosyVoice 配音 | ⚠️ 未配则 edge-tts 兜底可用 |

**唯一硬阻塞**：`LLM_API_KEY` 还是占位符。第 1/2/3 步凡是用到 AI 的功能都会 502。去 platform.deepseek.com 申请 key 填入并重启后端即可。

---

## 七、常见问题（踩过的坑）

| 现象 | 原因 | 解决 |
|---|---|---|
| celery 报 `Authentication required` | `.env` 改了没保存 / Redis 密码不符 | 确认磁盘 `.env` 已保存正确 REDIS_URL，重启服务 |
| 只有后端有反应、worker 没反应 | 多个项目共用同一 Redis 队列互抢 / worker 没起 | 保证只有一个本项目 worker，且后端在 Redis 可用时启动 |
| 任务一直 pending | 历史僵尸任务被前端恢复轮询 | 清浏览器 localStorage 的 `drama_task:` 项 |
| `NameError: Script not defined` / `No module named PIL` | 代码缺 import / 缺依赖 | 已修复；改代码后必须重启 worker |
| 首帧人物换脸/换现代装 | 没锁标准照 / 没填锚点 / lightning 底模遵循差 | 先填锚点+锁标准照；仍不稳则上 LoRA 或换写实底模 |
| 删除首帧报"未找到候选记录" | 首帧是直接引用的标准照而非候选图 | 已修复，按 URL 兜底清空 |

---

## 八、最短路径示例（第一次跑通建议照这个做）

1. 配好 `LLM_API_KEY`，重启后端+worker，确认 GPU 灯绿
2. 新建项目，画风选「古风水墨」
3. 剧本页：粘贴一段 3~5 个角色的武侠正文 → AI 生成分镜 → 批量提交
4. 素材页：点「提取素材」→ 检查每个角色的**英文锚点**是否准确（不准就编辑）
5. 标准照页：给每个主要角色「生成候选×4」→ 各挑一张「设为标准照」
6. 镜头页：逐个「生成首帧图×2」→ 选首帧 → 「生成视频×2」→ 通过
7. 配音页：批量配音
8. 合成页：导出成片 → 下载

走完这条链，你就得到了第一部角色一致的 AI 短剧。
