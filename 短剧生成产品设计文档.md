# AI 短剧生成平台 — 产品设计文档

> 版本：v1.0  
> 日期：2026-09-09  
> 目标：从「剧本 + 分镜」出发，检测内容缺陷，提取角色/场景/道具，生成素材清单与标准照，再结合分镜生成视频片段与配音，最终合成短剧成片。

---
 .\.venv\Scripts\uvicorn app.main:app --port 8000
## 一、GitHub 开源项目调研

以下为当前 GitHub 上与「AI 短剧 / 分镜生成」高度相关的开源项目，可作为本产品的技术参考与功能对标。

| 项目 | Star | 许可证 | 技术栈 | 核心能力 |
|---|---|---|---|---|
| [waoowaoo AI Studio](https://github.com/waooAI/waoowaoo) | 13.9k | CC BY-NC-SA 4.0（非商用） | Next.js 15 + React 19 + MySQL + Prisma + Redis + BullMQ + Tailwind v4 | 剧本解析、角色/场景生成、分镜视频、多角色配音、多语言 |
| [Huobao Drama 火宝短剧](https://github.com/chatfire-AI/huobao-drama) | 13k | CC BY-NC-SA 4.0（非商用） | Go + Vue3 + Mastra Agents | 一句话创意 → 剧本/角色/分镜/配音/视频/导出 |
| [Jellyfish](https://github.com/Forget-C/Jellyfish) | 6.3k | Apache-2.0 | FastAPI(Python) + React + Vite + MySQL + Redis + RustFS | 剧本拆解、资产一致性管理、镜头准备工作流、统一异步任务中心、模型/提示词管理 |
| [ArcReel](https://github.com/ArcReel/ArcReel) | 4.3k | AGPL-3.0 | Python + Claude Agent SDK + SQLite/PostgreSQL + Docker | 内容分析、资产管理、分镜、媒体生成、费用追踪、剪映草稿导出 |
| [story-shot-agent (PenShot)](https://github.com/neopen/story-shot-agent) | — | MIT | LangChain + LangGraph + Python | 多智能体协作：剧本→分镜→镜头级 Prompt，三视图一致性保障 |
| [shortdrama-pipeline](https://github.com/drasstry/shortdrama-pipeline) | — | — | Python (uv) | 主题→剧本→人物→分镜视频→单集成片的后端流水线 |
| [AIDrama Studio](https://github.com/EvoLinkAI/ai-short-drama) | — | MIT | Next.js 15 + Prisma | Novel→Script Analysis→Storyboard→Images→Videos→Voiceover→Final Drama |
| [CineGen AI Director](https://github.com/UllrAI/CineGen-ShortDrama) | — | — | React + Vite + Gemini 2.5 + Veo | Script-to-Asset-to-Keyframe 工业化工作流，角色一致性控制 |

**结论：** 商业友好的选型参考 **Jellyfish（Apache-2.0）** 和 **ArcReel（AGPL，注意网络传染义务）**；功能完整度参考 **waoowaoo**；分镜一致性方案参考 **story-shot-agent** 的多智能体 + 三视图设计。

---

## 二、产品整体流程

### 2.1 端到端流水线

```
┌──────────────────────────────────────────────────────────────────────┐
│                        AI 短剧生成平台 全流程                          │
└──────────────────────────────────────────────────────────────────────┘

  ① 输入剧本 + 分镜
        │
        ▼
  ② 内容检测（剧本/分镜不完善检测）
        │
        ▼
  ③ 素材提取（角色 / 场景 / 道具 描述 → 素材清单）
        │
        ▼
  ④ 标准照生成（角色图 / 场景图 / 道具图）  ←── 抽卡、重做、版本管理
        │
        ▼
  ⑤ 视频片段生成（标准照 + 分镜描述 → 3~8s 镜头） ←── 抽卡、验证、重生成
        │
        ▼
  ⑥ 配音生成（台词 → 各角色配音音频）
        │
        ▼
  ⑦ 合成导出（镜头拼接 + 配音对齐 + 字幕 + BGM → 成片 / 剪映草稿）
```

### 2.2 阶段状态机

每个阶段产物均有状态，支持「待处理 / 进行中 / 待审核 / 已通过 / 需重做」，便于人工介入审核与抽卡。

```
待处理 → 进行中 → 待审核 ──通过──→ 已通过
                  │
                  └──不通过──→ 进行中（重做）
```

---

## 三、前端页面布局与功能模块

### 3.1 整体布局

采用 **左侧导航 + 顶部项目栏 + 主工作区** 三段式布局。

```
┌─────────────────────────────────────────────────────────────────┐
│ 顶部栏：项目名称 │ 当前阶段进度条 │ 用户/设置                     │
├──────────┬──────────────────────────────────────────────────────┤
│          │                                                      │
│  左侧导航 │                  主工作区（按阶段切换）                 │
│          │                                                      │
│  📋 剧本  │                                                      │
│  🔍 检测  │                                                      │
│  🧩 素材  │                                                      │
│  🖼 标准照│                                                      │
│  🎬 镜头  │                                                      │
│  🔊 配音  │                                                      │
│  🎞 合成  │                                                      │
│  ⚙ 设置  │                                                      │
│          │                                                      │
└──────────┴──────────────────────────────────────────────────────┘
```

### 3.2 各页面功能详述

#### 页面 1：剧本与分镜输入

| 区域 | 功能 |
|---|---|
| 剧本输入区 | 支持粘贴文本 / 上传 .txt/.docx，按章节/场次组织 |
| 分镜输入区 | 表格形式：镜号、场景、画面描述、台词、时长、运镜、景别 |
| 导入模板 | 下载标准分镜 Excel 模板，批量导入 |
| 保存草稿 | 自动保存，支持版本回滚 |

**关键交互**：剧本与分镜可联动预览，左侧剧本高亮段落对应右侧分镜行。

#### 页面 2：内容检测

**检测维度（剧本 + 分镜）：**

| 检测项 | 说明 | 展示方式 |
|---|---|---|
| 角色一致性 | 同一角色名拼写/性别/身份是否前后矛盾 | 红/黄标注 + 建议修改 |
| 场景连续性 | 相邻分镜场景是否跳变无过渡 | 提示插入过场镜头 |
| 道具闭环 | 出现的道具是否在后续分镜中合理消失/使用 | 列出悬空道具 |
| 台词归属 | 台词是否有明确说话人 | 标记"无主台词" |
| 时长合理性 | 分镜时长是否在 3~8s 区间 | 超限标红 |
| 景别/运镜完整性 | 是否缺少景别或运镜描述 | 补全建议 |
| 剧情逻辑 | 时间线、因果关系是否通顺 | AI 生成问题清单 |

**页面布局**：左侧问题列表（按严重度分组），右侧原文对照 + 一键修复建议。

#### 页面 3：素材清单

**Tab 分组：角色 / 场景 / 道具**

| 字段 | 说明 |
|---|---|
| 名称 | 角色/场景/道具名 |
| 描述 | AI 提取的外观/特征描述，可手动编辑 |
| 参考图 | 上传参考图（可选） |
| 出现镜头 | 关联到哪些分镜 |
| 状态 | 待生成 / 已生成 / 已锁定 |

**操作**：`提取素材`（一键从剧本+分镜提取）、`编辑描述`、`合并同名项`、`删除`。

#### 页面 4：标准照生成

**以素材清单为输入，生成角色图 / 场景图 / 道具图。**

| 功能 | 说明 |
|---|---|
| 生成参数 | 风格（写实/动漫/国漫…）、画幅、模型选择、种子 |
| 批量生成 | 勾选多个素材一次性生成 |
| 抽卡模式 | 单素材生成 N 张候选（默认 4 张），用户选择满意的一张设为"标准照" |
| 版本管理 | 每次生成保留历史版本，可回滚 |
| 一致性约束 | 角色三视图（正面/侧面/背面）、统一画风 Tag |
| 锁定 | 锁定后下游视频生成强制引用该标准照 |

**页面布局**：卡片瀑布流，每张卡片显示素材名 + 候选图 + 操作（设为标准照/重生成/查看 Prompt）。

#### 页面 5：镜头视频生成

**输入：标准照 + 分镜描述 → 输出：3~8 秒视频片段。**

| 功能 | 说明 |
|---|---|
| 镜头列表 | 按镜号排列，展示分镜描述 + 关联标准照 |
| 生成参数 | 时长（3~8s）、分辨率、帧率、运镜、模型 |
| 抽卡验证 | 每个镜头生成 2~3 个候选视频，用户挑选通过的版本 |
| 状态机 | 待生成 → 生成中 → 待审核 → 通过/重做 |
| 预览 | 逐帧预览 + 播放 + 循环 |
| 批量操作 | 批量生成 / 批量通过 |
| 费用估算 | 生成前显示预计 Token / 费用 |

**页面布局**：上部分镜时间轴，下部镜头详情卡（左：分镜描述+标准照，右：候选视频+审核按钮）。

#### 页面 6：配音生成

**输入：台词 + 角色 → 输出：各角色配音音频。**

| 功能 | 说明 |
|---|---|
| 角色音色库 | 每个角色绑定一个音色（TTS 声音 ID / 克隆音色） |
| 台词列表 | 按镜号列出所有台词，显示说话人 + 文本 |
| 生成配音 | 单条 / 批量生成，支持语速、情绪、音调调节 |
| 试听 | 逐条试听，不满意可重生成 |
| 多语言 | 支持中/英/日等多语种配音 |
| 情感标签 | 开心/愤怒/悲伤…影响 TTS 情感参数 |

**页面布局**：左侧台词列表，右侧音频播放器 + 参数调节。

#### 页面 7：合成导出

| 功能 | 说明 |
|---|---|
| 时间轴 | 所有镜头视频 + 配音按顺序排列，可拖拽调整 |
| 对齐 | 配音自动对齐到对应镜头 |
| 字幕 | 自动生成字幕（基于台词），可编辑样式 |
| BGM | 上传/选择背景音乐，调节音量 |
| 转场 | 镜头间转场效果（淡入淡出/滑动…） |
| 导出成片 | MP4 导出，可选分辨率 |
| 导出剪映草稿 | 生成剪映工程文件，支持二次剪辑 |

---

## 四、后端技术栈与服务架构

### 4.1 技术栈选型

综合开源项目实践与可维护性，推荐以下技术栈：

| 层级 | 选型 | 理由 |
|---|---|---|
| 后端框架 | **FastAPI (Python)** | Jellyfish 验证；异步友好；自动 OpenAPI 文档；Python AI 生态成熟 |
| 前端框架 | **React 18 + Vite + TypeScript** | Jellyfish 采用；开发体验好；生态丰富 |
| 数据库 | **PostgreSQL** | ArcReel 支持；JSON 字段适合存储分镜/素材结构化数据；扩展性强 |
| 缓存/队列 | **Redis + Celery** | 用户熟悉 Celery；长耗时 AI 任务（图/视频生成）异步化必备 |
| 对象存储 | **MinIO / 阿里云 OSS** | 用户有 OSS 经验；存储图片、视频、音频 |
| ORM | **SQLAlchemy 2.0 + Alembic** | Python 生态主流 |
| 实时通信 | **WebSocket / SSE** | 推送任务进度（生成中→完成） |
| AI 编排 | **LangChain / LangGraph** | story-shot-agent 验证；多智能体协作（剧本解析/检测/分镜） |
| 视频处理 | **FFmpeg** | 镜头拼接、配音对齐、字幕烧录、格式转换 |
| 鉴权 | **JWT + OAuth2** | FastAPI 原生支持 |
| 部署 | **Docker Compose** | 所有参考项目均采用 |

### 4.2 服务架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Nginx (反向代理)                       │
└──────────────────────┬──────────────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
   ┌────▼────┐   ┌─────▼────┐   ┌─────▼──────┐
   │ Frontend│   │ FastAPI  │   │  WebSocket │
   │ (React) │   │ API 服务  │   │  实时推送   │
   └────┬────┘   └─────┬────┘   └─────┬──────┘
        │              │              │
        └──────────────┼──────────────┘
                       │
        ┌──────────────┼──────────────────────┐
        │              │                      │
   ┌────▼────┐   ┌─────▼─────┐          ┌─────▼──────┐
   │PostgreSQL│  │   Redis    │          │  MinIO/OSS  │
   │ (主数据库)│  │(缓存+队列) │          │ (媒体存储)   │
   └──────────┘  └─────┬─────┘          └────────────┘
                       │
                ┌──────▼──────┐
                │   Celery    │
                │  Worker 集群│
                └──┬───┬───┬──┘
                   │   │   │
          ┌────────▼┐ ┌▼────┐ ┌▼────────┐
          │ LLM Agent│ │图像  │ │视频/TTS  │
          │ (剧本解析 │ │生成  │ │生成 Worker│
          │  检测/分镜)│ │Worker│ │          │
          └───────────┘ └─────┘ └──────────┘
                   │
        ┌──────────┼──────────┬───────────┐
        │          │          │           │
   ┌────▼───┐ ┌────▼───┐ ┌────▼───┐ ┌─────▼────┐
   │ 文本LLM │ │ 文生图  │ │ 图生视频│ │  TTS 配音 │
   │(GPT/    │ │(SD/    │ │(Sora/   │ │(CosyVoice│
   │ DeepSeek│ │ Midj/   │ │ Veo/    │ │  鱼耳/    │
   │ /Claude)│ │ Flux)   │ │可灵)    │ │  火山)    │
   └────────┘ └────────┘ └────────┘ └──────────┘
```

### 4.3 核心模块划分

| 模块 | 职责 |
|---|---|
| `auth` | 用户注册/登录/JWT |
| `project` | 项目、章节管理 |
| `script` | 剧本、分镜 CRUD |
| `analysis` | 内容检测、素材提取（LLM Agent） |
| `asset` | 角色/场景/道具管理 |
| `image_gen` | 标准照生成（文生图 API 封装） |
| `video_gen` | 镜头视频生成（图生视频 API 封装） |
| `tts` | 配音生成（TTS API 封装） |
| `compose` | 合成导出（FFmpeg 封装） |
| `task` | 统一异步任务中心（Celery） |
| `model_provider` | 多模型供应商管理、密钥、费用统计 |
| `file` | 文件/媒体管理（OSS） |

---

## 五、核心调用流程

### 5.1 内容检测 + 素材提取

```
前端                              后端                          LLM
 │  POST /api/analysis/detect      │                             │
 │  {script, storyboard} ─────────►│                             │
 │                                 │  构造 Prompt（检测规则+剧本） │
 │                                 │────────────────────────────►│
 │                                 │◄──────── 检测结果 JSON ──────│
 │                                 │                             │
 │                                 │  构造 Prompt（提取素材）      │
 │                                 │────────────────────────────►│
 │                                 │◄── 角色/场景/道具 JSON ──────│
 │                                 │                             │
 │  返回 {issues, assets} ◄────────│                             │
 │  （SSE 流式返回检测过程）        │                             │
```

**LLM 输出 Schema（素材提取）：**

```json
{
  "characters": [
    {
      "name": "林夏",
      "gender": "female",
      "age": "25",
      "appearance": "长发披肩，白色连衣裙，气质清冷",
      "personality": "坚韧独立",
      "costume": "白色连衣裙"
    }
  ],
  "scenes": [
    {
      "name": "咖啡馆",
      "description": "暖色灯光，木质桌椅，窗外下雨",
      "time_of_day": "夜晚"
    }
  ],
  "props": [
    {
      "name": "黑色雨伞",
      "description": "长柄黑色雨伞，手柄有金色装饰"
    }
  ]
}
```

### 5.2 标准照生成流程

```
前端                              后端                  Celery              文生图 API
 │  POST /api/image/standard       │                     │                     │
 │  {asset_id, style, n_candidates}│                     │                     │
 │ ───────────────────────────────►│ 创建任务(进行中)     │                     │
 │                                 │────────────────────►│                     │
 │                                 │                     │ 构造 Prompt(描述+风格)│
 │                                 │                     │────────────────────►│
 │                                 │                     │◄──── N 张图片 URL ───│
 │                                 │  更新任务状态        │                     │
 │                                 │◄────────────────────│                     │
 │  WebSocket: task_done           │                     │                     │
 │ ◄───────────────────────────────│                     │                     │
 │                                 │                     │                     │
 │  GET /api/asset/{id}/candidates │                     │                     │
 │ ───────────────────────────────►│ 返回候选图列表        │                     │
 │                                 │                     │                     │
 │  POST /api/asset/{id}/select    │                     │                     │
 │  {candidate_id} ───────────────►│ 设为标准照(锁定)     │                     │
```

### 5.3 镜头视频生成流程

```
前端                              后端                  Celery              图生视频 API
 │  POST /api/shot/{id}/video      │                     │                     │
 │  {duration, model} ────────────►│ 创建任务            │                     │
 │                                 │────────────────────►│                     │
 │                                 │                     │ 加载标准照+分镜描述   │
 │                                 │                     │ 构造 Video Prompt    │
 │                                 │                     │────────────────────►│
 │                                 │                     │◄── 视频 URL(轮询) ───│
 │                                 │  更新任务            │                     │
 │  WebSocket: progress            │                     │                     │
 │ ◄───────────────────────────────│                     │                     │
 │                                 │                     │                     │
 │  POST /api/shot/{id}/approve    │                     │                     │
 │  {candidate_id} ───────────────►│ 镜头状态→通过        │                     │
```

### 5.4 配音生成流程

```
前端                              后端                  Celery              TTS API
 │  POST /api/tts/generate         │                     │                     │
 │  {character_id, text, voice_id} │                     │                     │
 │ ───────────────────────────────►│ 创建任务            │                     │
 │                                 │────────────────────►│                     │
 │                                 │                     │ 按角色音色调用 TTS    │
 │                                 │                     │────────────────────►│
 │                                 │                     │◄─── 音频 URL ────────│
 │                                 │  更新任务            │                     │
 │  WebSocket: done                │                     │                     │
 │ ◄───────────────────────────────│                     │                     │
```

### 5.5 合成导出流程

```
前端                              后端                  FFmpeg
 │  POST /api/compose/export       │                     │
 │  {project_id, format, params}   │                     │
 │ ───────────────────────────────►│ 创建任务            │
 │                                 │────────────────────►│
 │                                 │                     │ 1. 按序拼接镜头视频
 │                                 │                     │ 2. 配音对齐时间轴
 │                                 │                     │ 3. 烧录字幕
 │                                 │                     │ 4. 混合 BGM
 │                                 │                     │ 5. 输出 MP4 / 剪映草稿
 │                                 │  更新任务            │
 │  WebSocket: done                │
 │ ◄───────────────────────────────│
 │  GET 成片下载链接                │
```

---

## 六、数据模型设计（核心表）

### 6.1 ER 关系概览

```
Project 1──N Chapter 1──N Script
                              │
                              ├──N StoryboardShot (分镜)
                              │
                              ├──N Character (角色)
                              ├──N Scene (场景)
                              ├──N Prop (道具)
                              │
StoryboardShot 1──N VideoCandidate (视频候选)
Character 1──N ImageCandidate (标准照候选)
StoryboardShot 1──N Dialogue (台词) 1──1 Audio (配音)
```

### 6.2 核心表结构

**projects（项目）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| name | varchar | 项目名 |
| status | enum | 进行中/已完成 |
| style | varchar | 整体画风 |
| created_at | timestamp | |

**scripts（剧本）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | |
| project_id | UUID | |
| chapter | int | 章节 |
| content | text | 剧本文本 |
| storyboard | jsonb | 分镜数组 |

**characters / scenes / props（素材）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | |
| project_id | UUID | |
| name | varchar | 名称 |
| description | text | 描述 |
| reference_image | varchar | 参考图 URL |
| standard_image | varchar | 锁定的标准照 URL |
| status | enum | 待生成/已生成/已锁定 |

**image_candidates（标准照候选）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | |
| asset_type | enum | character/scene/prop |
| asset_id | UUID | |
| image_url | varchar | 图片 URL |
| prompt | text | 生成 Prompt |
| seed | int | 种子 |
| model | varchar | 模型 |
| is_selected | bool | 是否被选为标准照 |

**storyboard_shots（分镜）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | |
| script_id | UUID | |
| shot_no | int | 镜号 |
| scene | varchar | 场景 |
| description | text | 画面描述 |
| dialogue | text | 台词 |
| duration | float | 时长(秒) |
| camera_movement | varchar | 运镜 |
| shot_size | varchar | 景别 |
| status | enum | 待生成/生成中/待审核/通过/重做 |
| characters | jsonb | 关联角色 ID 数组 |

**video_candidates（视频候选）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | |
| shot_id | UUID | |
| video_url | varchar | 视频 URL |
| duration | float | |
| model | varchar | |
| prompt | text | |
| status | enum | 待审核/通过/淘汰 |

**dialogues（台词）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | |
| shot_id | UUID | |
| character_id | UUID | 说话人 |
| text | text | 台词文本 |
| emotion | varchar | 情绪 |
| audio_url | varchar | 配音 URL |
| voice_id | varchar | 音色 ID |

**tasks（异步任务）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | |
| type | enum | detect/extract/image/video/tts/compose |
| status | enum | pending/running/success/failed/cancelled |
| progress | int | 0-100 |
| result | jsonb | 结果 |
| error | text | 错误信息 |
| created_at | timestamp | |

---

## 七、AI 模型供应商对接

### 7.1 模型能力映射

| 能力 | 推荐供应商（国内可用） | 备选 |
|---|---|---|
| 剧本解析/检测/分镜 | DeepSeek V3 / Qwen-Max / GPT-4o | Claude 3.5 Sonnet |
| 文生图（标准照） | 通义万相 / 即梦 / Midjourney API | Stable Diffusion (本地) |
| 图生视频（镜头） | 可灵 / 即梦视频 / Vidu | Sora / Veo 3（需海外） |
| TTS 配音 | 鱼耳 TTS / CosyVoice / 火山引擎 | ElevenLabs |

### 7.2 供应商抽象层

设计统一的 Provider 接口，前端通过下拉选择模型，后端按 provider 路由：

```python
class BaseProvider:
    def text(self, prompt, **kwargs) -> str: ...
    def image(self, prompt, **kwargs) -> list[str]: ...   # 返回图片 URL 列表
    def video(self, image, prompt, **kwargs) -> str: ...  # 返回视频 URL
    def tts(self, text, voice_id, **kwargs) -> str: ...   # 返回音频 URL
```

---

## 八、关键设计要点

### 8.1 一致性保障
- **角色三视图**：角色标准照生成时，同时生成正面/侧面/背面，作为视频生成的参考图输入。
- **统一风格 Tag**：项目级画风 Tag（如 `国风水墨, 高画质, 电影感`）注入所有图/视频 Prompt。
- **锁定机制**：标准照锁定后，下游强制引用，避免角色漂移。

### 8.2 抽卡与审核
- 每个生成节点均为「生成 N 候选 → 用户选 1 → 锁定」模式。
- 任务状态机驱动，不通过则回到生成态。

### 8.3 费用与成本
- 每次生成记录 Token / 调用次数 / 实际费用到 `tasks` 表。
- 项目级费用看板：统计剧本/图像/视频/配音各自消耗。

### 8.4 长任务处理
- 所有 AI 生成走 Celery 异步队列。
- WebSocket 实时推送进度（生成中、排队中、完成）。
- 支持任务取消与失败重试。

---

## 九、部署方案（Docker Compose）

```yaml
services:
  frontend:   # React 构建产物 + Nginx
  backend:    # FastAPI + Uvicorn
  celery_worker:
  celery_beat:
  postgres:
  redis:
  minio:      # 或外接阿里云 OSS
  ffmpeg:     # 独立服务或内置到 backend 镜像
```

**端口分配：**
- 前端：80
- 后端 API：8000
- WebSocket：8000（同 FastAPI）
- PostgreSQL：5432
- Redis：6379
- MinIO：9000

---

## 十、开发优先级建议

| 阶段 | 内容 | 预估 |
|---|---|---|
| P0 | 项目/剧本/分镜 CRUD + 内容检测 + 素材提取 | 2 周 |
| P1 | 标准照生成（文生图）+ 抽卡锁定 | 2 周 |
| P2 | 镜头视频生成（图生视频）+ 抽卡审核 | 2 周 |
| P3 | TTS 配音生成 | 1 周 |
| P4 | 合成导出（FFmpeg 拼接 + 剪映草稿） | 2 周 |
| P5 | 费用统计、模型管理、权限、优化 | 2 周 |

---

## 十一、本地部署方案（开源模型路线）

> 核心思路：用 ComfyUI + 开源模型替代商业 API，将边际成本压到接近零，只需承担 GPU 硬件/租赁费用。

### 11.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│              短剧生成平台（FastAPI + React）              │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP API 调用
        ┌────────────┼────────────┬───────────────┐
        │            │            │               │
  ┌─────▼─────┐ ┌────▼─────┐ ┌────▼──────┐ ┌─────▼─────┐
  │  ComfyUI  │ │  ComfyUI │ │ CosyVoice │ │  Ollama /  │
  │ (文生图)  │ │ (图生视频)│ │  (TTS)    │ │ vLLM(LLM) │
  └─────┬─────┘ └────┬─────┘ └────┬──────┘ └─────┬─────┘
        │            │            │               │
  ┌─────▼─────┐ ┌────▼─────┐ ┌────▼──────┐ ┌─────▼─────┐
  │ SD1.5/XL  │ │ Wan2.2   │ │ CosyVoice │ │ Qwen /    │
  │ + LoRA    │ │ GGUF版   │ │ 2 0.5B    │ │ DeepSeek  │
  └───────────┘ └──────────┘ └───────────┘ └───────────┘
```

### 11.2 硬件配置建议

| 配置档 | GPU | 显存 | 内存 | 存储 | 适用场景 |
|---|---|---|---|---|---|
| 入门 | RTX 3060 / 4060 | 12 GB | 32 GB | 200 GB SSD | SD1.5 + Wan2.2(量化) + CosyVoice |
| 推荐 | RTX 4070 Ti Super | 16 GB | 64 GB | 500 GB SSD | SDXL/FLUX + Wan2.2 14B + 全部模型 |
| 顶配 | RTX 4090 | 24 GB | 64 GB | 1 TB SSD | 全模型无压力，速度最快 |
| 云租赁 | AutoDL/趋动云 4090 | 24 GB | — | — | ¥2~5/小时，按需使用 |

> 若只有 8 GB 显存：SD1.5 + Wan2.2 GGUF Q4 量化 + CPU offload 也能跑，只是速度慢。

### 11.3 各环节模型选型与部署

#### ① 文生图（标准照）— ComfyUI + Stable Diffusion

| 组件 | 选型 | 说明 |
|---|---|---|
| 基础模型 | SD 1.5（6GB 显存）/ SDXL（8GB+）/ FLUX（12GB+） | 画质 FLUX > SDXL > SD1.5 |
| 平台 | **ComfyUI** | 节点式工作流，支持 API 调用 |
| 角色一致性 | **LoRA + InstantID + IP-Adapter** | 三件套锁定角色身份 |
| LoRA 训练工具 | **Kohya_ss** / FluxGym | 15~30 张参考图训练角色 LoRA |

**部署步骤：**
```bash
# 1. 安装 ComfyUI
git clone https://github.com/comfyanonymous/ComfyUI.git
cd ComfyUI && pip install -r requirements.txt

# 2. 安装 ComfyUI Manager
cd custom_nodes
git clone https://github.com/ltdrdata/ComfyUI-Manager.git

# 3. 下载基础模型（放 models/checkpoints/）
# SD1.5: v1-5-pruned-emaonly.safetensors (~4GB)
# SDXL: sd_xl_base_1.0.safetensors (~7GB)

# 4. 启动（开启 API 模式）
python main.py --listen 0.0.0.0 --port 8188 --enable-cors-header "*"
```

**角色一致性工作流（生产级）：**
```
训练 LoRA（Kohya_ss，15~30张参考图，30分钟~1小时）
  → 加载 Checkpoint + LoRA（触发词如 linxia_character）
  → InstantID 注入角色面部特征
  → IP-Adapter 微调细节
  → ControlNet 控制姿势
  → KSampler 采样
  → FaceDetailer 脸部修复
  → 输出标准照
```

#### ② 图生视频（镜头）— ComfyUI + Wan2.2

| 组件 | 选型 | 说明 |
|---|---|---|
| 模型 | **Wan2.2-Animate**（通义万相开源版） | 国产开源，质量接近商业模型 |
| 版本 | 5B 版（24GB）/ 14B GGUF 量化版（8~16GB） | 量化版牺牲少量画质换显存 |
| 加速 | **LightX2V LoRA** | 20 步采样降到 6 步，速度提升 3 倍 |
| 显存优化 | GGUF 量化（Q4/Q5）+ CPU offload | 8GB 显卡可跑 480P~720P |

**所需模型文件：**
| 文件 | 位置 | 说明 |
|---|---|---|
| Wan2.2 高噪声 GGUF（Q4） | models/unet/ | 处理运动和构图 |
| Wan2.2 低噪声 GGUF（Q4） | models/unet/ | 处理细节和纹理 |
| LightX2V I2V LoRA | models/loras/ | 步数蒸馏加速 |
| Wan2.1 Text Encoder | models/text_encoders/ | 文本编码 |
| Wan2.1 VAE | models/vae/ | 视频编解码 |

**ComfyUI 自定义节点（必装）：**
- `ComfyUI-GGUF`（GGUF 模型加载）
- `ComfyUI-VideoHelperSuite`（视频输出）
- `ComfyUI-Easy-Use`（LoRA 管理）
- `rgthree-comfy`（上下文管理）

**显存与速度参考：**
| 显卡 | 分辨率 | 5秒视频耗时 |
|---|---|---|
| RTX 4090 (24GB) | 720P | ~3~5 分钟 |
| RTX 3090 (24GB) | 720P | ~5~8 分钟 |
| RTX 4070 (12GB) | 480P | ~6~10 分钟 |
| RTX 3060 (12GB) | 480P | ~10~15 分钟 |

#### ③ TTS 配音 — CosyVoice / Fish Speech

| 模型 | 显存 | 特点 |
|---|---|---|
| **CosyVoice2 0.5B** | ~5 GB | 阿里开源，3 秒音色克隆，中文效果最好 |
| CosyVoice3 0.5B | ~5 GB | 升级版，多语言更强 |
| Fish Speech S2 Pro | ~6 GB | 开源权重可自部署，盲测评分第一 |

**部署（CosyVoice，Docker 方式）：**
```bash
# 克隆项目
git clone https://github.com/FunAudioLLM/CosyVoice.git
cd CosyVoice

# 构建 Docker 镜像
cd docker && docker build -t cosyvoice:latest .

# docker-compose 启动
docker compose up -d
# 服务默认监听 8080 端口
```

**关键能力：**
- 零样本音色克隆：3~10 秒参考音频即可克隆角色声音
- 情感控制：通过指令文本控制情绪（开心/愤怒/悲伤）
- 多语言：中/英/日/韩等

#### ④ LLM（剧本解析/检测）— 本地或 API

| 方案 | 模型 | 显存 | 说明 |
|---|---|---|---|
| 本地部署 | Qwen2.5-7B / DeepSeek-V3 | 16GB（量化后 8GB） | 用 Ollama 或 vLLM 部署 |
| 商业 API | DeepSeek V3 / Qwen-Max | — | 成本极低（¥1/百万 tokens），建议直接用 API |

> LLM 环节成本占比不到 1%，建议直接用商业 API，省去部署麻烦。

### 11.4 与产品系统的集成方式

所有本地模型均通过 **HTTP API** 被后端调用，后端封装统一的 Provider 接口：

```python
class LocalComfyUIProvider:
    """文生图 + 图生视频，调用 ComfyUI API"""
    base_url = "http://localhost:8188"

    def generate_image(self, prompt, lora_name, **kwargs) -> list[str]:
        # 构造 ComfyUI 工作流 JSON
        # POST /prompt 提交任务
        # 轮询 /history/{prompt_id} 获取结果
        # 返回图片 URL 列表

    def generate_video(self, image_url, prompt, duration, **kwargs) -> str:
        # 调用 Wan2.2 I2V 工作流
        # 返回视频 URL


class LocalCosyVoiceProvider:
    """TTS 配音"""
    base_url = "http://localhost:8080"

    def tts(self, text, voice_id, emotion, **kwargs) -> str:
        # POST /tts 调用 CosyVoice API
        # 返回音频 URL
```

**ComfyUI API 调用示例：**
```python
import requests

# 提交文生图任务
workflow = {...}  # ComfyUI 工作流 JSON
resp = requests.post("http://localhost:8188/prompt", json={"prompt": workflow})
prompt_id = resp.json()["prompt_id"]

# 轮询结果
while True:
    history = requests.get(f"http://localhost:8188/history/{prompt_id}").json()
    if prompt_id in history:
        outputs = history[prompt_id]["outputs"]
        break
    time.sleep(2)
```

### 11.5 成本估算（本地部署路线）

按 100 集、每集 18 个镜头（90 秒）计算：

| 项目 | 自有 4090 | 云 GPU 租赁（AutoDL） |
|---|---|---|
| GPU 成本 | 仅电费 ~¥50/月 | ¥3/小时 × 100 小时 = ¥300 |
| 文生图（约 200 张） | 电费 | 含在 GPU 费 |
| 视频生成（1800 镜头 × 5s） | 电费 | 含在 GPU 费 |
| TTS 配音（全集约 2 万字） | 电费 | 含在 GPU 费 |
| **100 集合计** | **≈ ¥50~100** | **≈ ¥300~500** |

> 若采用混合方案（重要镜头用商业 API，其余本地生成），成本可控制在 ¥1000~3000/季。

### 11.6 本地 vs 商业 API 对比

| 维度 | 本地部署 | 商业 API |
|---|---|---|
| 单集成本 | ¥0.5~5 | ¥50~720 |
| 100 集成本 | ¥50~500 | ¥5,000~72,000 |
| 画质 | 中（Wan2.2 ≈ 商业 720P） | 高（可灵/即梦 1080P+） |
| 速度 | 慢（单卡排队） | 快（云端并行） |
| 一致性 | 好（LoRA 锁角色） | 中（需参考图控制） |
| 上手难度 | 高（部署+调参） | 低（调 API） |
| 隐私 | 全本地，数据不出域 | 数据上传到服务商 |

---

## 十二、云 GPU 部署实战指南（AutoDL / 趋动云）

> 以 AutoDL 4090（24GB）为例，趋动云操作类似。目标：在一台云 GPU 上跑起 ComfyUI（文生图+图生视频）+ CosyVoice（TTS），并对外提供 API。

### 12.1 整体规划

| 服务 | 端口 | 模型 | 显存占用 |
|---|---|---|---|
| ComfyUI（文生图+图生视频） | 8188 | SDXL + Wan2.2 14B | ~18 GB |
| CosyVoice（TTS） | 8080 | CosyVoice2 0.5B | ~5 GB |
| 短剧平台后端 | 8000 | FastAPI | ~2 GB（CPU） |

> 4090 共 24GB 显存，ComfyUI 跑视频时占 ~18GB，剩余 6GB 给 CosyVoice 刚好。若显存紧张，可让 ComfyUI 跑视频时临时卸载，或用 CPU offload。

### 12.2 第一步：创建 AutoDL 实例

1. **注册充值**：[AutoDL 官网](https://www.autodl.com/) 注册，充值 50~100 元
2. **选择镜像**：推荐使用社区镜像 **「ComfyUI 云绘通用版」**（已预装 ComfyUI + 60+ 工作流 + Wan2.2/SD/FLUX 模型）
   - 镜像地址：`https://www.autodl.com/market/58`
3. **选择 GPU**：RTX 4090（24GB），按量计费约 ¥2.5/小时
4. **系统盘**：选 100GB 以上（模型文件大）
5. **创建并开机**

### 12.3 第二步：启动 ComfyUI

实例创建后，通过 **JupyterLab** 或 **SSH** 进入：

```bash
# 通过 JupyterLab 启动（推荐）
# 1. 点击实例的「JupyterLab」入口
# 2. 打开 启动.ipynb，运行第一个单元格
# 3. 点击「启动 ComfyUI」按钮

# 或通过命令行启动
cd /root/ComfyUI
python main.py --listen 0.0.0.0 --port 8188 --enable-cors-header "*"
```

启动后，通过 **AutoDL 自定义服务** 访问 8188 端口，即可打开 ComfyUI Web 界面。

### 12.4 第三步：下载模型文件

ComfyUI 镜像可能已预装部分模型，若缺失，通过工具箱下载：

```bash
# 在 JupyterLab 的 工具箱.ipynb 中运行下载器
# 或直接用 huggingface-cli 下载

# 1. Wan2.2 图生视频模型（放 models/unet/）
# 高噪声 + 低噪声 GGUF Q8 版本（24GB 显存用 Q8，16GB 用 Q4）
huggingface-cli download nahz202/Wan_2.2_Repackaged \
  --include "wan2.2_i2v_high_noise_14B_q8_0.gguf" \
  --local-dir /root/ComfyUI/models/unet

huggingface-cli download nahz202/Wan_2.2_Repackaged \
  --include "wan2.2_i2v_low_noise_14B_q8_0.gguf" \
  --local-dir /root/ComfyUI/models/unet

# 2. LightX2V 加速 LoRA（放 models/loras/）
huggingface-cli download nahz202/Wan_2.2_Repackaged \
  --include "wan2.2_i2v_lightx2v_4steps_lora_v1_high_noise.safetensors" \
  --include "wan2.2_i2v_lightx2v_4steps_lora_v1_low_noise.safetensors" \
  --local-dir /root/ComfyUI/models/loras

# 3. Text Encoder（放 models/text_encoders/）
huggingface-cli download nahz202/Wan_2.2_Repackaged \
  --include "umt5_xxl_fp8_e4m3fn_scaled.safetensors" \
  --local-dir /root/ComfyUI/models/text_encoders

# 4. VAE（放 models/vae/）
huggingface-cli download nahz202/Wan_2.2_Repackaged \
  --include "wan_2.1_vae.safetensors" \
  --local-dir /root/ComfyUI/models/vae

# 5. SDXL 基础模型（放 models/checkpoints/）
huggingface-cli download stabilityai/stable-diffusion-xl-base-1.0 \
  --include "sd_xl_base_1.0.safetensors" \
  --local-dir /root/ComfyUI/models/checkpoints
```

### 12.5 第四步：安装 ComfyUI 自定义节点

```bash
cd /root/ComfyUI/custom_nodes

# GGUF 模型加载（Wan2.2 必需）
git clone https://github.com/city96/ComfyUI-GGUF

# 视频处理
git clone https://github.com/Kosinkadink/ComfyUI-VideoHelperSuite

# LoRA 管理
git clone https://github.com/yolain/ComfyUI-Easy-Use

# 角色一致性
git clone https://github.com/cubiq/ComfyUI_InstantID
git clone https://github.com/cubiq/ComfyUI_IPAdapter_plus

# 脸部修复
git clone https://github.com/Gourieff/ComfyUI-Impact-Pack

# 重启 ComfyUI 生效
```

### 12.6 第五步：部署 CosyVoice（TTS）

由于 ComfyUI 已占用大部分显存，CosyVoice 可以**同机部署**（显存允许时）或**单独租一台小卡**（如 A10/3060）。

**方案 A：同机 Docker 部署（显存允许时）**

```bash
# 克隆 CosyVoice2-Ex（已封装好的部署版）
git clone https://github.com/Fakeboxs/CosyVoice2-Ex-GONGJI.git
cd CosyVoice2-Ex-GONGJI

# 一键部署
chmod +x deploy.sh && ./deploy.sh

# 服务启动在 7865 端口（WebUI）和 8080 端口（API）
```

**方案 B：同机手动部署**

```bash
# 1. 创建独立 conda 环境
conda create -n cosyvoice python=3.10 -y
conda activate cosyvoice

# 2. 克隆代码
git clone https://github.com/FunAudioLLM/CosyVoice.git
cd CosyVoice
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

# 3. 下载模型（ModelScope 国内加速）
pip install modelscope
python -c "
from modelscope import snapshot_download
snapshot_download('iic/CosyVoice2-0.5B', local_dir='./pretrained_models/CosyVoice2-0.5B')
"

# 4. 启动 API 服务
cd runtime/python/fastapi
python server.py --host 0.0.0.0 --port 8080
```

> **显存分配技巧**：启动 CosyVoice 时设置 `CUDA_VISIBLE_DEVICES=0`，并通过 `torch.cuda.set_per_process_memory_fraction(0.2)` 限制显存占用，避免与 ComfyUI 抢显存。

### 12.7 第六步：网络配置与 API 访问

AutoDL 默认不暴露端口，需要配置：

**方式一：AutoDL-SSH-Tools（推荐，最简单）**

1. 下载 [AutoDL-SSH-Tools](https://autodl-public.ks3-cn-beijing.ksyuncs.com/tool/AutoDL-SSH-Tools.zip)
2. 输入实例的 SSH 账号密码
3. 配置端口转发：8188→本地 8188，8080→本地 8080，8000→本地 8000
4. 本地浏览器访问 `http://127.0.0.1:8188` 即可

**方式二：配置自定义服务端口**

在 AutoDL 控制台 → 实例详情 → **自定义服务** 中添加端口映射：
- 8188 → ComfyUI
- 8080 → CosyVoice API
- 8000 → 短剧平台后端

**方式三：Nginx 反代（生产环境）**

```bash
# 安装 Nginx
apt install nginx -y

# 配置反代
cat > /etc/nginx/conf.d/ai.conf << 'EOF'
server {
    listen 80;
    location /comfy/ { proxy_pass http://127.0.0.1:8188/; }
    location /tts/   { proxy_pass http://127.0.0.1:8080/; }
    location /api/   { proxy_pass http://127.0.0.1:8000/; }
}
EOF
nginx -s reload
```

### 12.8 第七步：短剧平台后端对接

在后端配置中指定本地模型地址：

```python
# config.py
class Config:
    COMFYUI_BASE_URL = "http://127.0.0.1:8188"
    COSYVOICE_BASE_URL = "http://127.0.0.1:8080"
    LLM_API_URL = "https://api.deepseek.com"  # LLM 用商业 API
    LLM_API_KEY = "sk-xxx"
```

后端通过 ComfyUI API 提交工作流，通过 CosyVoice API 生成配音。

### 12.9 费用优化技巧

| 技巧 | 说明 |
|---|---|
| **无卡模式下载** | 下载模型时用「无卡模式」开机（仅 ¥0.1/小时），下载完再切到 GPU 模式 |
| **用完即关** | 不用时关机，AutoDL 关机不收 GPU 费用（只收存储费 ~¥0.1/天） |
| **批量生成** | 攒一批任务一起跑，减少开机/关机的模型加载时间 |
| **定时任务** | 用脚本自动开机→跑任务→关机，实现无人值守 |
| **共享镜像** | 把配置好的环境保存为自定义镜像，下次直接用，省去重装时间 |

### 12.10 常见问题排查

| 问题 | 解决方案 |
|---|---|
| CUDA out of memory | Wan2.2 换 Q4 量化版，或开启 `--lowvram` 模式 |
| ComfyUI 启动慢 | 首次加载模型正常，后续用 `--fast` 模式 |
| CosyVoice 与 ComfyUI 抢显存 | 设 `CUDA_MEMORY_FRACTION=0.2` 限制 CosyVoice |
| API 调用超时 | 视频生成耗时长，后端设超时 600s，用 WebSocket 推送进度 |
| 模型下载慢 | 用 ModelScope 或 AutoDL 社区模型库，不要直接用 HuggingFace |

---

## 附录：参考资源

- [Jellyfish](https://github.com/Forget-C/Jellyfish) — Apache-2.0，FastAPI + React，最贴近本产品定位
- [ArcReel](https://github.com/ArcReel/ArcReel) — 剪映草稿导出、费用追踪
- [waoowaoo](https://github.com/waooAI/waoowaoo) — Next.js 全栈，功能完整度参考
- [story-shot-agent](https://github.com/neopen/story-shot-agent) — LangGraph 多智能体分镜方案
- [awesome-ai-short-drama](https://github.com/PAMPAS-Lab/awesome-ai-short-drama) — 项目合集
